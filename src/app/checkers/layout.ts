import { Metadata } from "next/types";
import Page from "./page";

export const metadata: Metadata = {
  title: "Checkers",
  description: "Checkers Game with Alpha-Beta Algorithm",
};

export default Page;
