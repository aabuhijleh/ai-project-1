import { Metadata } from "next/types";
import Page from "./page";

export const metadata: Metadata = {
  title: "Knapsack",
  description: "Knapsack Problem Solver using Simulated Annealing",
};

export default Page;
